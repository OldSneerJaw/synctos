{
  "name": "couchbase-sync-maker",
  "version": "0.1.0",
  "dependencies": { },
  "devDependencies": {
    "expect.js": "0.3.1",
    "mocha": "2.2.5",
    "simple-mock": "0.3.1"
  },
  "scripts": {
    "test": "mkdir -p target/test-reports/ && ../sync-function-maker.rb sample-sync-doc-definitions.js target/test-sample-sync-function.js && node_modules/.bin/mocha --recursive -R xunit test/ > target/test-reports/sync-function-maker.xml"
  }
}
