{
  "name": "synctos",
  "version": "0.1.0",
  "dependencies": { },
  "devDependencies": {
    "expect.js": "*",
    "mocha": "*",
    "simple-mock": "*"
  },
  "scripts": {
    "test": "mkdir -p target/test-reports/ && ./make-sync-function samples/sample-sync-doc-definitions.js target/test-sample-sync-function.js && node_modules/.bin/mocha --recursive -R xunit test/ > target/test-reports/sync-function-maker.xml"
  }
}
